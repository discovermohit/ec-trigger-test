name: Your GHA
on:
  pull_request:
    types: [opened, synchronize]

jobs:
  your_job_name:
    name: Trigger Jenkins Job
    runs-on: ubuntu-latest
    steps:
      - name: Trigger your-awesome-job-name job
        uses: toptal/jenkins-job-trigger-action@master
        with:
          jenkins_url: "http://54.209.68.230:8080/"
          jenkins_user: "Mohit"
          jenkins_token: ${{ secrets.TOKEN }}
          job_name: "Application/job/Dev/job/analytics-engine/"
          job_timeout: "3600"
